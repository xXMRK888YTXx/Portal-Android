<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PortalApp"
        android:allowBackup="false"
        android:appComponentFactory="com.xxmrk888ytxx.portal.PortalComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Portal"
        tools:replace="android:appComponentFactory">
        <activity
            android:name=".view.mainActivity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Portal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.unlockScreenActivity.UnlockScreenActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:showForAllUsers="true"
            android:theme="@style/Theme.Portal">
            <intent-filter>
                <action android:name="com.xxmrk888ytxx.portal.UNLOCK_REQUEST_FROM_PC" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.fastUnlockActivity.ShortcutUnlockActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:theme="@style/Theme.Portal.Transparent">
            <intent-filter>
                <action android:name="com.xxmrk888ytxx.portal.UNLOCK_FROM_SHORTCUT_ACTION" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".data.broadcastReceiver.ShortcutPinnedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xxmrk888ytxx.portal.ACTION_SHORTCUT_PINNED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".data.service.UnlockFromShortcutService"
            android:exported="false"
            android:foregroundServiceType="shortService">
            <intent-filter>
                <action android:name="com.xxmrk888ytxx.portal.SHORTCUT_UNLOCK_ACTION" />
            </intent-filter>
        </service>

        <service
            android:name=".data.service.WOLUnlockService"
            android:exported="false"
            android:foregroundServiceType="shortService">
            <intent-filter>
                <action android:name="com.xxmrk888ytxx.portal.WOL_UNLOCK_ACTION" />
            </intent-filter>
        </service>

        <receiver
            android:name=".data.broadcastReceiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />
    </application>

</manifest>